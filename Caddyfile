# Caddyfile (Final Working Version)
:8080 {
    # This is the new, simple instruction.
    # It tells Caddy to forward any request starting with /api
    # to your back-end server, keeping the path intact.
    reverse_proxy /api/* localhost:8000

    # This is the default rule for the front-end.
    reverse_proxy localhost:3000
}